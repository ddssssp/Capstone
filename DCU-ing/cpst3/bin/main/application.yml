spring:
  # DB 접속 정보 설정
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://sugang.cvco2wowy2u6.ap-northeast-2.rds.amazonaws.com/sugang
    username: admin
    password: "00000000"

  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  #서버 설정
  server:
    servlet:
      session:
        #timeout:1800
        tracking-modes: cookie
    port: 8081